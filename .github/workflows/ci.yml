name: CI

on:
  push:
    branches:
      - main
    tags-ignore:
      - v*
      
env:
  GITHUB_TOKEN: ${{ github.token }}
  
jobs:
    nightly:
        name: Nightly
        runs-on: windows-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v3.0.2
              with:
                submodules: 'false'
                fetch-depth: 0
 
            - name: Bump version in AssemblyVersionInfo.cs
              id: versioning
              uses: gittools/actions/gitversion/execute@v0.9.7
              with:
                additionalArguments: '/showConfig'
                useConfigFile: true
                updateAssemblyInfo: true
                updateAssemblyInfoFilename: src/Properties/AssemblyInfo.cs
                
            - name: Bump version in manifest.json
              uses: jossef/action-set-json-field@v2
              with:
                file: src/manifest.json
                field: version
                value: ${{ steps.versioning.outputs.fullSemVer }}

            - name: Commit changes
              run: |
                git config --local user.email "action@github.com"
                git config --local user.name "GitHub Action"
                git add -A
                git commit -m "🔖 bump version to ${{ steps.versioning.outputs.fullSemVer }}"
                
            - name: Push changes to manifest.json
              uses: ad-m/github-push-action@v0.6.0
              with:
                github_token: ${{ github.token }}
                branch: ${{ github.ref }}
                tags: ${{ env.REPO }}:latest,${{ env.REPO }}:v${{ steps.versioning.outputs.fullSemVer }}
                
            - name: Create Release
              if: ${{ !startsWith(github.ref, 'refs/tags/') }}
              uses: ncipollo/release-action@v1.10.0
              with:
                tag: v${{ steps.versioning.outputs.fullSemVer }}
                prerelease: true
